<%= form_for @question do |f| %>
  <%= render 'common/form_errors', object: @question %> 

  <p>
    <%= f.label :body, "Question" %>
    <%= f.text_field :body %>

    <%= f.submit "Ask a Question", :disable_with => 'Submiting...' %>
  </p>
<% end %>
